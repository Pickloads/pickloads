# Module Index — M-00 … M-43 · Upgrade M-50 … M-62 · M-69 → M-82 →

One row per shipped module. Every module passed the full gate (functionality
· responsiveness · WCAG AA · SEO · security · typecheck · lint · build; test
suites from M-40 on). Details live in each module's doc.

**Project status: RESUMED past M-62.** M-62 closed the upgrade directive and
was the final module *of that cycle*; work has since restarted against
[`docs/FINAL-IMPLEMENTATION-PLAN.md`](../FINAL-IMPLEMENTATION-PLAN.md),
which scopes M-69 … M-101 across three build cycles. **M-69 (Production
Integrity Pack) is the first module of the new programme** — Phase A, a
prerequisite that repairs seven live defects (§3, P-1…P-7) before any new
feature is built on them.

**M-70 opens Phase B** — tracking core (M-70 … M-79). It shipped the shipment
domain layer (types, DTO serializers, tracking-number generator); **M-71**
lands the tables those types specify, their RLS and the §5 immutability
trigger; **M-72** adds `shipment_events`, the §20 transition engine and the
atomic write path — and, with it, the **integration test lane**
(`npm run test:integration`) that `FINAL-IMPLEMENTATION-PLAN` §4 restores as
the dropped §27 tier; **M-73** ships the public `/track` route — the
programme's first new PUBLIC ATTACK SURFACE — with migration 0020's access
ledger, the §4 two-factor lookup and the `shipment` i18n namespace in five
locales. The shipper portal is M-74; the dispatcher board is M-75; **M-76**
adds §13's carrier update surface and the unauthenticated
`/driver/update/[token]` — the programme's SECOND public attack surface, and
the first bearer credential in the schema (migration 0023, with the repo's
first column-level privilege revoke). **M-77** closes §16 with migration 0024:
a second private bucket, the **document-type → audience MATRIX as data** (in
TypeScript *and* as rows, pinned to each other), and the completion of the
`pod_uploaded` precondition M-72 deliberately deferred to it. **M-78** lands
§10's ETA architecture and §21's exception lifecycle (migration 0025).
**M-79 CLOSES PHASE B** with migration 0026 — §17's eleven customer
notifications and the **background processing architecture** the plan's §4
records as an unjustified silent downgrade: a queue table, an append-only
attempt ledger, a harvest watermark and a `CRON_SECRET`-guarded worker route,
so shipment sends stop being inline.

**M-80 OPENS PHASE C** (tracking completion) with migration 0027 — the two
tables M-71 left to it (`shipment_locations`, `tracking_provider_connections`),
§9's four privacy levels wired on the WRITE side, the §9 Mode C **adapter
interface** for Motive/Samsara/Geotab/Verizon Connect with **no provider
connected and no fake connection**, the lazy-loaded map and its accessible text
equivalent — and the **location-history retention EXECUTOR** the plan's §4
records as "a policy with no purger", proved by deletion. **M-81** adds §12's
broker-partner access with migrations 0028–0029 — admin-invited only,
organization-scoped, verification as a database gate, §12's **two** sharing
shapes as two tables, and the **allow/deny permission lists as data** that the
plan's §4 restores as a dropped requirement. **M-82** closes the plan's §4
row *"§22 — 12 breakpoints (audit reused the existing 7) — UNJUSTIFIED,
unstated"* by auditing every tracking surface at all twelve widths **in a
browser, behind the real compiled stylesheets** — which is what the previous
six modules' jsdom scans structurally could not do — and fixes the twelve
defects that exposed.

Current totals: **388 pages** built · **1462** unit tests · **283** e2e tests ·
**742** RLS isolation assertions · **329** integration tests. M-62-era baseline
for comparison: 337 pages · 168 · 145 · 165. Upgrade-directive acceptance is unchanged:
[`docs/UPGRADE-ACCEPTANCE.md`](../UPGRADE-ACCEPTANCE.md) — 17 ✅, 8 ⚠️
(live-environment dependencies, all listed in the runbook's post-cutover
checklist), 0 ❌.

| Module | Phase | Doc | Summary |
|---|---|---|---|
| M-00 | 0 | [M-00-foundations.md](M-00-foundations.md) | Repo, tooling, Next.js 15 + Tailwind v4 setup, V4 design tokens in `globals.css`, security headers, env scaffolding |
| M-01 | 0 | [M-01-database.md](M-01-database.md) | Full schema (13 tables, enums, triggers), RLS on everything (no anon inserts), private storage bucket, `company_settings` switchboard, seed |
| M-02 | 0 | [M-02-auth-core.md](M-02-auth-core.md) | Supabase clients (browser/server/admin), session middleware, `/portal` protection |
| M-02b | 2 | [M-02b-login-and-types.md](M-02b-login-and-types.md) | Portal sign-in page + generated DB types for Phase 2 tables |
| M-10/11 | 1 | [M-10-11-home.md](M-10-11-home.md) | V4 component library + home page (hero, ticker, services, pricing, quick-quote, compliance, packet) — pixel-faithful conversion |
| M-12 | 1 | [M-12-interior-pages.md](M-12-interior-pages.md) | About, Contact, FAQ, Shippers, legal shells (noindex until counsel-approved) |
| M-13 | 1 | [M-13-i18n.md](M-13-i18n.md) | next-intl routing (en default, es/fr/ru/ht prefixed), V4-dictionary extraction pipeline, `useV4()` bridge |
| M-13b | 1 | [M-13b-i18n-migration.md](M-13b-i18n-migration.md) | Completed string migration across all interior pages |
| M-14 | 1 | [M-14-forms.md](M-14-forms.md) | Live lead capture: quick-quote + contact + newsletter + freight quote — rate-limit → Turnstile → Zod → service-role insert → Resend, `email_log` journal |
| M-15 | 1 | [M-15-seo-analytics.md](M-15-seo-analytics.md) | Metadata, hreflang, sitemap/robots, JSON-LD, consent-gated GA4 |
| M-16 | 1 | [M-16-equipment-pages.md](M-16-equipment-pages.md) | 8 `/dispatch/[equipment]` landing pages with original copy |
| M-20/21 | 2 | [M-20-21-onboarding-uploads.md](M-20-21-onboarding-uploads.md) | Become-a-carrier wizard (4 steps), secure document uploads, EIN encryption (S-01) |
| M-22 | 2 | [M-22-esign-webhook.md](M-22-esign-webhook.md) | Dropbox Sign agreement flow + HMAC-verified idempotent webhook |
| M-23 | 2 | [M-23-crm.md](M-23-crm.md) | Leads pipeline (kanban), activities journal, auto status journaling |
| M-24 | 2 | [M-24-admin-dashboard.md](M-24-admin-dashboard.md) | Admin dashboard (sales + operations) and company-settings editor |
| M-25 | 2 | [M-25-carrier-portal.md](M-25-carrier-portal.md) | Carrier portal v1: documents, agreement status, replacements |
| M-26 | 2 | [M-26-new-authority-page.md](M-26-new-authority-page.md) | `/start-your-trucking-company` funnel, `lead_type=new_authority` + auto-tagging |
| M-30 | 3 | [M-30-loads.md](M-30-loads.md) | Loads CRUD + status machine (booked→…→paid, cancellable until money moves) |
| M-31 | 3 | [M-31-stripe.md](M-31-stripe.md) | Stripe dispatch-fee-only invoicing + signature-verified idempotent webhook |
| M-32 | 3 | [M-32-shipper-portal.md](M-32-shipper-portal.md) | Shipper portal v1 (email-matched quotes), role-routed portal homes |
| M-33 | 3 | [M-33-blog-cms.md](M-33-blog-cms.md) | Staff blog editor + publish workflow, escape-first markdown renderer, public blog with Article JSON-LD |
| M-34 | 3 | [M-34-dashboard-dispatch-marketing.md](M-34-dashboard-dispatch-marketing.md) | Dashboard: dispatch KPIs, marketing modules, notifications feed |
| M-35 | 3 | [M-35-state-pages-crons.md](M-35-state-pages-crons.md) | 6 `/truck-dispatch/[state]` pages + daily cron (insurance alerts, callback digest, CRON_SECRET-guarded) |
| M-40 | H | [M-40-unit-tests.md](M-40-unit-tests.md) | Vitest: 76 tests — validation schemas, i18n slug parity, markdown XSS, load state machine, PII crypto, guard degradation |
| M-41 | H | [M-41-e2e-smoke.md](M-41-e2e-smoke.md) | Playwright: 12 chromium smoke tests against the production build, secretless (~13s) |
| M-42 | H | [M-42-password-recovery-i18n.md](M-42-password-recovery-i18n.md) | `/forgot-password` + `/reset-password` flows, login link, 10 supplemental es/fr dictionary strings |
| M-43 | H | [M-43-launch-runbook.md](M-43-launch-runbook.md) | Launch runbook (`docs/LAUNCH-RUNBOOK.md`), README deployment section, this index |
| M-50 | U | [M-50-data-model.md](M-50-data-model.md) | Upgrade data model: migrations 0005–0009 (shippers, memberships + backfill, account status/history, audit, preferences, fleet, support, notifications, invoices, `freight_quotes.shipper_id` FK + RLS), extended DB types |
| M-51 | U | [M-51-portal-selection.md](M-51-portal-selection.md) | Pre-auth `/portal` selection page (two V4 cards), header Login + Get Started, mobile menu + Support entries, real topbar/footer auth links (Coming-Soon toasts removed) |
| M-52 | U | [M-52-create-account-carrier.md](M-52-create-account-carrier.md) | `/create-account` chooser (D1-gated shipper door) + carrier registration with authority-status routing (onboarding / pending / new-authority / leased-on manual review), never-auto-confirmed signups, verified-email loop |
| M-53 | U | [M-53-shipper-registration.md](M-53-shipper-registration.md) | Shipper registration (industry/frequency/regions, quote-request wording), server-side role promotion, post-verification quote claiming + RLS-scoped shipper portal reads (admin-client workaround retired for self-signups) |
| M-54 | U | [M-54-role-redirects.md](M-54-role-redirects.md) | Role-aware redirects everywhere (login fallback → `/portal` router, authed auth-pages bounce), central suspension enforcement, expired-session + suspended + unverified-email states, role-integrity verified (unit + PG trigger checks) |
| M-55 | U | [M-55-carrier-portal-completion.md](M-55-carrier-portal-completion.md) | Carrier portal completion: overview dashboard, D5 profile editing + change requests, trucks/drivers CRUD, agreements + re-send, invoices mirror surface, notifications, support threads (+staff inbox), account settings; migration 0010 |
| M-56 | U | [M-56-shipper-portal-completion.md](M-56-shipper-portal-completion.md) | Shipper portal completion: overview with brokerage-gated tracking waitlist, full professional quote form (0011 fields, membership-verified insert), quotes status timeline, honest documents/billing states, support, company + account settings |
| M-57 | U | [M-57-membership-architecture.md](M-57-membership-architecture.md) | Membership doctrine surfaced app-wide: wizard claims now write owner memberships, last profile_id lookups converted (loads page, billing email, cron alerts — owner-first with legacy fallback), statically pinned by test; invite-teammate extension path documented |
| M-58 | U | [M-58-admin-account-management.md](M-58-admin-account-management.md) | Admin account management: /portal/admin/users (filter/paginate, approve/suspend + history/audit/email/notification, onboarding progress, dispatcher assignment), tokenized single-use staff invites (0012), security log, dispatcher least-privilege query scoping |
| M-59 | U | [M-59-responsive-a11y.md](M-59-responsive-a11y.md) | Responsive + WCAG 2.2 AA sweep: V4 media-block ordering root-cause fix, off-canvas portal drawer, table→card transform, skip links, contrast tokens, axe e2e scan over 16 pages |
| M-60 | U | [M-60-email-suite.md](M-60-email-suite.md) | Customer email suite: 15 localized React Email builders on a shared CustomerEmail layout, `notify.ts` fan-out (notification + email + journal), wired across signup/onboarding/documents/agreements/billing/quotes/support; admin quotes desk + carrier activate toggle |
| M-61 | U | [M-61-security.md](M-61-security.md) | Security audit: staff TOTP MFA (`/portal/admin/mfa`, admin hard / dispatcher 14-day grace, central gate in `requireStaff`/`requireAdmin`), 165-assertion RLS isolation suite (`npm run test:rls`) that caught + fixed a live anon-grant defect (0013), audit_events gaps closed, secret/TTL/error-leak sweep, [SECURITY-REVIEW.md](../SECURITY-REVIEW.md) |
| M-62 | U | [M-62-qa-finalization.md](M-62-qa-finalization.md) | **Final module.** Responsive suite (`tests/e2e/responsive.spec.ts`, 108 tests: 21 routes × 375/390/768/1024/1440 with full-page screenshots + 320/1920 endpoint sweep; no-overflow and nav clip/overlap assertions, injected-regression validated; baseline PNGs deliberately not committed — 33 MB, decision documented); [UPGRADE-ACCEPTANCE.md](../UPGRADE-ACCEPTANCE.md) walking all 25 directive §24 criteria (17 ✅ / 8 ⚠️ live-env / 0 ❌); runbook rewritten for M-50…M-61 (migrations 0005–0013 order + per-migration rollback, audited env table incl. 3 declared-but-unused vars, Supabase auth email templates, staff MFA + two-admin rule, in-app invite flow, 9 `company_settings` keys, `npm run test:rls` as a release gate, post-cutover checklist); INDEX + README |

| M-69 | A | [M-69-production-integrity.md](M-69-production-integrity.md) | **First module past M-62.** Production Integrity Pack — the seven live defects in `FINAL-IMPLEMENTATION-PLAN` §3: tokenized `/newsletter/unsubscribe` (5 locales, GET-renders/POST-acts, idempotent, rate-limited) + RFC 8058 one-click endpoint and `List-Unsubscribe` header pair, closing the CAN-SPAM gap the confirmation email promised (P-1, migration 0014 — dedicated `unsubscribe_token`, rationale documented); the sitewide referral-bonus promise gated behind a new `referral_program_active` key with the approved copy and all five translations kept intact (P-2, 0015 + seed); the ungated "Freight Brokerage" footer label gated on `brokerage_active` with the already-approved "For Shippers" fallback (P-3); all 10 direct `audit_events` inserts across 4 action files routed through `src/lib/audit.ts` with unchanged semantics, plus an ESLint `no-restricted-syntax` rule (injection-validated) that keeps it that way (P-4); `document.download` journalled on the carrier path — actor/document/carrier, never the signed URL (P-5); `packet_downloads_live` and `testimonials_visible` wired from dead config to real behaviour through a new fail-closed `company-settings` accessor, with the V4 testimonial markup restored behind a double lock and zero sample content (P-6); `formatRpm` → `formatLoadedRpm` with every label corrected, new `formatTrueRpm` and nullable `loads.deadhead_miles` + capture field — no displayed value silently changed (P-7, 0016) |
| M-70 | B | [M-70-shipment-domain.md](M-70-shipment-domain.md) | **Opens Phase B.** Shipment domain foundation — no migrations, no routes, no UI: `src/lib/shipments/types.ts` (§6's 18 statuses in lifecycle order behind a `satisfies Record<ShipmentStatus, …>` guard; §7 event type/source/visibility with a **fifth `broker` band** added for the same reason `FINAL-IMPLEMENTATION-PLAN` §4 flags on `doc_visibility` — without it §12's "BOL, when authorized" is unimplementable; §9 tracking modes + the four location-visibility levels; §10 ETA source/confidence; §16 document types + visibility; §21's 13 exception types + severity; 10 row types M-71's DDL is written from, incl. `ShipmentRow` (§18 expanded, `@staffOnly` tags on `gross_shipper_amount`/`carrier_pay`/`margin`) and `ShipmentEventRow` with all 18 §7 fields incl. `idempotency_key`/`external_event_id`/`metadata`; `statusKey()` and siblings returning i18n KEYS — no catalogue entries, those land with M-73's UI in five locales); `tracking-number.ts` (`PL-YYYY-######` — CSPRNG sequence with rejection sampling, UTC year, tolerant normalisation ↔ canonical storage, strict rejection incl. adjacent-year and overflow, and the SQL pattern + unique-index + immutability-trigger names exported so M-71's DDL cannot drift; the guessing mitigation documented honestly — the number is an identifier, never a credential, per §5's own "secure secondary verification"); `dto.ts` (five audience serializers built by **explicit allow-list construction** — no spread, no `delete` — so a future column defaults to invisible; §4's forbidden list absolute for the public audience, `public_access_hash` serialized by nobody including staff, staff-only event bands unreachable from any customer timeline, §9's four privacy levels applied per audience with `exact` capped at city/state for public visitors). +77 unit (**268**) incl. key-set equality per audience, a sentinel sweep over serialized JSON, a `@staffOnly` static scan, a structural guard on `dto.ts` itself, and three anti-vacuity checks proving the safety assertions can fail. 173 RLS + 160 e2e unchanged (no schema, no surface) |
| M-71 | B | [M-71-shipment-schema.md](M-71-shipment-schema.md) | **The tables M-70 specified.** Migrations 0017–0018, additive, 0001–0004 untouched: all **17** shipment enum types created up front from M-70's `as const` arrays (value lists and order pinned by the M-70 unit tests) so M-72/M-77/M-78/M-80 add tables, never a second vocabulary; `shipments` (every one of `ShipmentRow`'s 52 columns in declaration order — privacy-first defaults: `public_tracking_enabled` false, `location_visibility` `approximate`), `shipment_parties` (polymorphic `organization_id` deliberately without an FK — a receiving dock has no account), `shipment_assignments` (+ a partial unique index making "reassignment is a new row" a database fact, not a comment); §5's three tracking-number guarantees under the exported names — CHECK on `TRACKING_NUMBER_SQL_PATTERN`, unique `shipments_tracking_number_key`, and `trg_shipments_tracking_number_immutable`, which holds against the **service role** because `BYPASSRLS` is not `BYPASSTRIGGER` and disabling a trigger needs table ownership (proved as the table owner with RLS bypassed); §2's legal gate implemented as `trg_shipments_brokerage_gate` — INSERT-only so in-flight freight stays operable if the flag is switched back off, fail-closed when the key is missing, and explicitly one layer of a two-layer control M-75 completes in the service layer; **17 documented indexes** per §25, seven of them partial; RLS on all five tables with **zero anon policies** (§19 forbids anonymous shipment SELECT — M-73 goes through the service role) and **no customer write policy at all**, which is what makes §19's "carrier users cannot edit financial fields" true by construction; new `broker_partners` + `broker_partner_memberships` + active-filtered `my_broker_partner_ids()` — the minimum §12's "explicitly linked" broker RLS needs, with M-81 still owning invitation, verification and sharing grants; `user_role` deliberately NOT extended (broker access is organization-scoped). +**107** RLS assertions (**280**), covering shipper/carrier/broker A-vs-B, an unapproved broker org granting nothing, anon reading nothing from five tables, non-member insert rejection, and every constraint/trigger asserted as the table owner — **four injected defects each failed the suite loudly** before being removed. Two residual risks named for M-83: financial columns are not column-protected (R-1) and dispatcher scoping stays query-level (R-2) |
| M-72 | B | [M-72-transition-engine.md](M-72-transition-engine.md) | **The §20 engine and the timeline it writes.** Migration 0019, additive: `shipment_events` with all 18 §7 fields (incl. `metadata jsonb not null default '{}'`, a **globally** unique `idempotency_key` so a retried write cannot double-append, and a **per-shipment** unique `external_event_id` for M-80's Mode C provider dedupe), six documented indexes led by `(shipment_id, event_time desc, id desc)` — the `id` tiebreaker is what makes §25's keyset timeline pagination stable — plus `trg_shipment_events_append_only`, which refuses UPDATE and DELETE for **every** role including the service role (`BYPASSRLS` is not `BYPASSTRIGGER`), making §7's "do not delete event history silently" a database fact with one documented consequence: a shipment carrying events can no longer be deleted. RLS mirrors `AUDIENCE_EVENT_VISIBILITY` verbatim — shipper `public`+`shipper`, carrier `public`+`carrier`, broker `public`+`broker`, **`staff_only` in no customer policy**, zero anon policies — and a unit test **parses the migration** to prove the SQL and `dto.ts` have not drifted. `src/lib/shipments/transitions.ts` follows the `LOAD_TRANSITIONS` idiom at scale: a 47-edge `Record<ShipmentStatus, …>` that is deliberately **not** M-70's declaration order (`delayed` is a detour reachable from and returning to every operational status; `delivered` has no edge to `cancelled`), §20's seven preconditions, the 17-pair impossible-transition list led by the directive's own `delivered → carrier_search`, and a §19 actor gate limiting carriers and drivers to operational reporting; every rejection is a typed, explainable failure with the offending edge and the unmet preconditions, never a silent no-op, and the full `Record` types make a nineteenth status a compile error. `apply-transition.ts` resolves every §20 fact in ONE query (§25: no N+1), asks the engine before touching the database, then writes the status change **and** its event through a `security definer` RPC — one transaction, plus a **compare-and-swap** two PostgREST round trips cannot have, so a losing concurrent writer gets a typed `PL409` instead of a lost update; EXECUTE is granted to `service_role` alone, so even an admin session is refused (asserted). §20's controlled admin correction is admin-only, demands a reason at three layers, and is an **additional** `correction` event plus an `audit_events` row through the M-69 single writer — never an edit. Appointments are event-sourced per plan §4: set/reschedule writes the column and an old→new event in one statement, so a reschedule has history instead of an overwritten column. §14's `record call` / `record email` / public-update-vs-internal-note get typed events (the UI stays M-75's). §26 ships as call-site shape only (plan C-5: Sentry does not exist) — nine named signals, allow-list fields with no payload escape hatch, credential-shaped detail dropped whole. +**85** unit (**353**), +**59** RLS (**339**), and a **new integration lane** (33 tests) proving create→assign→event→status-update and idempotent replay against local PG16 with the real engine driving the real SQL. **Five injected defects each failed loudly** before removal — one of them caught by the unit suite with no database at all |
| M-73 | B | [M-73-public-tracking.md](M-73-public-tracking.md) | **The public `/track` page — the programme's first new public attack surface.** Migration 0020, additive: `shipment_tracking_access`, the §19 access ledger, with **one** policy (staff SELECT), **no anon policy and no write policy at all** — every row arrives through the service role, and `trg_shipment_tracking_access_append_only` refuses UPDATE/DELETE for the table owner too, because a tamperable enumeration log reads as evidence while being none. The table stores the attempted tracking NUMBER and the attempted secondary value **in no form whatsoever**: M-70's row type has no field, the DDL has no column, `recordTrackingAccess` has no parameter, and RLS §9a asserts the **exact 8-column set** so a future `secondary_hash` fails the suite (a ledger of hashed ZIPs would be a rainbow-friendly index of exactly the credential §4 relies on). The §4 credential is **HMAC-SHA-256 under `TRACKING_ACCESS_SECRET`**, not a plain digest — ~41 000 live US ZIPs make `sha256(zip)` a one-second rainbow table, and the first factor is printed on every BOL; the module **fails CLOSED** with the key unset, because "cannot verify" is not "verified". Guard stack in order: rate limit **4/10 min per IP** (tighter than the shared 5, with the per-number limit deliberately rejected as a customer-facing DoS and replaced by a per-number ledger index) → Turnstile → Zod (permissive on shape on purpose: rejecting a malformed year with its own message would confirm which years are live) → non-short-circuiting `timingSafeEqual` → `toPublicTrackingDto`. **Enumeration**: unknown number, wrong secondary value and admin-suspended tracking return the *identical frozen value* and render the *identical sentence*, the miss branch runs a full HMAC against a per-process CSPRNG **decoy** so it is not the fast path, and every outcome is held to a 350 ms floor; a failed ledger write **refuses a correct lookup**, because §19 says the route logs access, not that it tries to. A **server action, not a route handler** — no URL means no tracking number in a `Referer`, a history entry, a proxy log or a search index, and the page itself is a static shell that reads no shipment, so §25's "never cache private shipment data publicly" is true by construction rather than by directive. §8's four blocks (header / nine-milestone timeline / summary / contact) over a `MILESTONE_PROGRESS` `satisfies Record<ShipmentStatus, number>` guard that makes a nineteenth status a compile error and keeps a delayed truck's completed ticks. §23: `<ol>` + `<time datetime>`, every step's state in **visible text** ("Completed"/"Current step"/"Not started"), and the mandated **text equivalent** doubling as the `role="status"` live region; `<details>` support disclosure, no modal, no focus trap. **Decision D-6 implemented as recommended**: a 29-phrase curated library translated ×5, stored as `phrase:<id>` tokens in the columns the migrations already ship, plus an honest `lang="en"` + "Written by dispatch, in English" fallback for genuinely novel prose — nothing is translated at request time, and a retired token degrades *visibly*. §8's support button **reuses** `submitContactMessage` (plan R-1: `support_threads.profile_id` is NOT NULL, D-5 assigns guest tickets to M-89) with the subject composed server-side so a reference cannot be forged. §30's six honest labels ×5 with the copy audited three ways against "live tracking"/"AI-powered" — and a §30 finding filed against M-12's `/shippers` marketing copy rather than silently rewritten. The **`shipment` i18n namespace M-70 deferred**: 176 keys × 5 locales in the generator (en/es/fr authored, ru/ht mirroring English and flagged), covering 18 statuses, 20 event types, 13 exceptions, 4 severities, 9 milestones, 29 phrases and all page copy. +**84** unit (**437**) incl. the DTO call-site proof M-70's doc said its own suite could not give, +**18** RLS (**357**), +**14** integration (**47**) running the real lookup against the real schema through a psql-backed PostgREST adapter, +**14** e2e (**174**) incl. axe on `/track` and the result view axe-scanned in four states. **Five injected defects each failed loudly**; page count 343 → **348** |
| M-74 | B | [M-74-shipper-shipments.md](M-74-shipper-shipments.md) | **§11's shipper surface.** `/portal/shipper/shipments` + `[shipmentId]`, with the three items the plan's §4 table restored — invoice status, shipment contacts, update history — all shipped. Migration **0021**, additive except for one deliberate relaxation the RLS suite forced: the first draft kept 0008's `invoices.carrier_id NOT NULL` and named the hauling carrier on a shipper invoice, which 0009's shipped `"member read invoices"` policy (keyed on `my_carrier_ids()`) would have made **readable by that carrier** — handing them `amount_cents`, i.e. the shipper gross, and with their own `carrier_pay` the margin §18 marks staff-only and §12 puts on the broker must-not-see list. The fix is structural rather than a filter: a shipper invoice names **no** carrier, so the carrier policy cannot match it under any query, and the NOT NULL is replaced by `invoices_party_present` (`carrier_id is not null or shipper_id is not null`) — the invariant that actually mattered. Plus `shipment_id`/`shipper_id`, two partial indexes and ONE read policy; 0009's carrier policy left byte-identical and every pre-0021 row unchanged and newly visible to nobody. **§25 enforced, not asserted**: `src/lib/shipments/shipper-list.ts` holds the bound as a constant and a recording query builder proves no reachable path selects `shipments` without a `range()` spanning ≤ `MAX_PAGE_SIZE` (`?page=1e9` clamps rather than becoming a 10⁹-row OFFSET); the **summary-vs-history split** is structural — `getShipmentSummary` touches `shipment_events` *not at all* (asserted as an exact four-call chain, and again against real SQL), while history is keyset-paginated at 25 (+1 lookahead) behind `?before=`; the eight §11 tiles are `head:true` COUNT queries so a dashboard loads no row, and all four detail reads share one `Promise.all`. §11's nine filters with free text **allow-listed rather than escaped** — PostgREST's `or()` grammar means a comma reshapes the filter, so `x,status.eq.completed` becomes a literal search that matches nothing (proved against the real database). §11's ten detail blocks including an honest **map slot** (M-80's; §30's milestone label, and asserted to contain no `canvas`/`iframe`/`img`/`svg` and none of live-tracking/real-time/AI copy), a read-only **documents** empty state (M-77's), **invoice status** read from `invoices` under 0021's own policy, and **contacts** applying M-71's `public_contact` rule — the carrier row's channels withheld with *"Contact through dispatch"* rather than a blank, the company name kept. **M-73 reused, not duplicated**: `buildPublicTimeline`/`TrackingTimeline` widened to a structural `TimelineSubject` both DTOs satisfy, so one implementation of §8's nine milestones serves `/track` and the portal; the D-6 phrase library resolves identically in both; the `shipment` namespace gains only `party.*` ×6, and **ten labels were deliberately NOT re-declared** after the generator was caught about to overwrite the V4 prototype's ru/ht wording. §2 gate evaluated in **both** directions — waitlist when brokerage is off and the account has no freight, the real list when it has (M-71 made the DB gate INSERT-only so in-flight freight stays operable; hiding it in CSS would be the same mistake). Overview **extended, not replaced**: the four M-56 quote tiles stay and §11's "pending quotes" IS the shipped "Pending review". Every "not yours" is a **404, never a 403**. +**141** unit (**578**), +**29** RLS (**386**), +**31** integration (**78** — §27's *portal lookup*, the real `src/` functions against the real schema as a real `authenticated` session through a new RLS-applying psql adapter), +**5** e2e (**179**); page count 348 → **353**. Non-vacuity by injection: the app-level `.eq("shipper_id")` predicate **removed** and RLS alone still refuses, with an admin control proving the assertion can fail — and two real defects the suites caught before ship (the invoice disclosure above, and `operatingDayBounds` sampling the DST offset at the wrong instant) |
| M-75 | B | [M-75-dispatcher-operations.md](M-75-dispatcher-operations.md) | **§14's dispatcher surface.** `/portal/admin/shipments` (board + §5 search), `/new` (create + `?quote=` conversion) and `/[shipmentId]` (the fourteen §14 actions), plus migration **0022** — **four `security definer` functions and nothing else**: no table, no policy, no enum, no trigger, no index. Each exists for M-72's stated reason — the operation is two-to-three writes that must be one transaction — and the sharpest is `assign_shipment_carrier`, where a crash between the assignment row and `shipments.carrier_id` would manufacture a *permission bug* (0018's carrier policy keys on that column, so the carrier just assigned could not see the shipment). `create_shipment`'s INSERT is **dynamic on purpose**: `jsonb_populate_record` NULLs every omitted key and would override 0017's privacy-first defaults, and restating them here would fork the DDL — so the column list is the intersection of the payload keys and `information_schema.columns`, identifiers from the **catalog** and values as one bound `jsonb` parameter. Five keys (`id`, `created_at`, `updated_at`, `completed_at`, `cancelled_at`) are stripped in **both** TypeScript and SQL, the second layer proved by sending them past the first. **§2's service-layer half, which M-71 explicitly assigned here**: `assertBrokerageOpen()` refuses before a tracking number is minted, fails closed on an unreadable switchboard, and carries a staff-readable sentence naming the business fact, the switch and what still works — with `P0001` from 0017's trigger mapped back to the same words, and the refusal proved to issue **zero** round trips and **zero** audit rows. §5's number is minted by M-70's generator with a **23505 retry that re-rolls only on `shipments_tracking_number_key`** (a different unique violation is a real error, not a collision). **Every status change goes through M-72's engine and nothing else** — no raw UPDATE exists in the module; appointments call `set_shipment_appointment`, §20's correction calls `apply_shipment_correction` (admin-only at the surface *and* in the engine, with a `min(10)` reason bound a human reads), and `record call` / `record email` / public-update-vs-internal-note / `request POD` / exception logging all call `append_shipment_event` with M-70's existing event types — structured facts in `metadata`, never buried in prose, with `event_time` (when the call happened) kept apart from `recorded_at` (when it was typed up). **§14's eight-column board, server-side**: column membership is data, every column is its own bounded+counted+scoped query, eight fixed queries in one `Promise.all`, `MAX_PAGE_SIZE` **re-exported from M-74** so a second ceiling cannot exist, and M-74's §11 filter builder reused verbatim. It renders M-23's `.kanban` vocabulary and **adds no CSS and no colour at all** (asserted by walking the rendered DOM against `portal.css`) but drops the drag-to-move: a gesture cannot carry a cancellation reason, assert closeout or surface a refusal, so status moves are explicit controls over `availableTransitions(...)` — M-72's own instruction to M-75. **§14's Realtime sentence answered with a reason**: no subscription, because RLS-filtered broadcast would push dispatcher B's freight to dispatcher A (dispatcher scoping is query-level, M-71's R-2), because the expensive part is the eight counts, and because M-72's compare-and-swap makes stale *safe* — `PL409` renders as "Somebody else moved this shipment — reload", closing M-72's R-4. **§19 honoured, not widened** (zero new policies): `shipmentScopeExpression` has **two** arms — assigned carriers *and* own `dispatcher_id`, because §6's first four statuses have no carrier and a carrier-only rule would blind a dispatcher to the freight they are sourcing — applied to the board, to §5 search (otherwise the search box defeats every scoped board) and to the **carrier dropdown**, whose unscoped version would be privilege escalation through a `<select>`; and `resolveShipmentAccess` re-reads session + shipment + scope before **all fifteen** server actions. §5 search normalises a mangled paste and additionally **reconstructs** `PL 2026 000458` / `2026-000458`, which M-70's normaliser cannot (it folds separators, it does not insert them), while only digits survive so `%` and an injection attempt alike become `none`. **Two deferrals argued rather than announced**: `shipment_exceptions` is NOT half-created (M-71's own rule) — an exception is an `exception_opened` event with §21's type and severity structured in `metadata` under a named `m75_event_only` marker M-78 backfills from, and `resolve exception` is explicitly not shipped; the ETA writes only M-71's shipped columns plus an `eta_update` event carrying the **previous** value, with `DISPATCHER_ETA_SOURCES` withholding `calculated`/`provider` because §30 forbids labelling a typed guess as a computed prediction. Notification resend is portal-feed-only with an M-79-shaped idempotency key, and the UI says so. +**221** unit (**799**), +**11** RLS (**397**), +**50** integration (**128** — §27's *dispatcher flow* end to end, the seventh of the eleven), +**8** e2e (**187**); page count 353 → **363**. **Five injected defects each failed loudly**, including one that produced five simultaneous failures because the action suite *discovers* its exports rather than listing them — plus two real defects the lanes caught pre-ship (a six-digit search read as a year, and an idempotency-key UPDATE that 0019's append-only trigger refuses) |
| M-76 | B | [M-76-carrier-driver-updates.md](M-76-carrier-driver-updates.md) | **§13's carrier update surface and the driver link.** `/portal/carrier/shipments` + `/[shipmentId]` (RLS-scoped through `my_carrier_ids()` on the cookie-bound client, `toCarrierDto` output only) and the unauthenticated `/driver/update/[token]`, plus migration **0023** — one enum, two tables, six indexes, two triggers, three policies, four `security definer` functions and **the repo's first column-level privilege revoke**. **The token is opaque by construction**: 32 CSPRNG bytes, base64url, derived from nothing — §13's *"do not expose internal shipment IDs in predictable URLs"* read as a constraint on the token's SHAPE, which is why a signed JWT was rejected (it puts the id in a decodable segment and makes revocation a denylist) and why the unit suite asserts across **1000 mints** that no token contains any fragment of the shipment id, carrier id or tracking number. Stored as an HMAC under `DRIVER_TOKEN_SECRET` that **fails closed** — no key, no mint, no verify — and `token_hash` is **revoked at COLUMN level** from `authenticated` and `anon` (revoke-then-grant, because a table-level SELECT overrides a column revoke), so M-71's residual risk R-1 does not apply to the one table in the schema where it would have mattered: three independent guarantees (privilege · the `Omit` type · the projection). **Redemption is ONE SQL statement** — rate check, lookup, expiry, revocation, carrier re-check, usage bump and ledger insert — because done in the app they are three round trips with two race windows, and because §13 demands the same event be rate limited AND audit logged: one write serving both is the only way they cannot drift. **The rate limit is the LEDGER, not Upstash**, and the reason is stated rather than assumed — `checkRateLimit` is a documented no-op without Redis credentials, which is fine for a lead form and not fine for the enumeration budget on a bearer credential; two counters (8 failures / 60 total per IP / 10 min) over one window, with a per-TOKEN limit deliberately refused because it would let anyone holding a link lock the driver out. §13's list is **data**, and four of its phrases are not §6 statuses (`loaded`→`loading`, `departed pickup`→`picked_up`, `confirm dispatch`→`dispatched`), with `unloading` added because §6's graph routes `arrived_at_delivery → unloading → delivered` and §13 says *"may include"*. **One reviewed widening of M-72's actor gate**: `dispatched` for `carrier` only — confirming dispatch commits a company to freight, and the driver token is a leakable bearer credential in a truck — turning M-72's `driver === carrier` assertion into the stronger strict-subset-plus-one-named-difference. **§9/§13 consent is actively granted**: the column defaults to `pending`, the checkbox starts unticked, `denied` is a first-class reversible choice, and a location supplied without consent is **refused and journalled**, never silently dropped — while a driver's ability to report a STATUS is never gated. **§30's `label.tracking_link_expired` finally has a call site** — M-73 authored it ×5 and recorded that it had none — rendered identically for four genuinely different causes (unknown · expired · revoked · carrier released), with the four action results asserted **deeply equal** so no caller can turn the page into an existence oracle. **§22 phone-first, argued**: one column at every width, 56px controls (a third above WCAG 2.5.8's floor, for a gloved thumb), radios instead of a native `<select>`, 16px inputs so iOS cannot zoom the viewport, **zero `:hover` rules in the whole block**, and **320px added to the responsive suite for every route**. **113 i18n keys × 5 locales** with the server actions returning message KEYS, not English — drivers are the population §24 exists for. BOL/POD upload is M-77's and both surfaces say so. +**151** unit (**966**), +**50** RLS (**447** — §12 proves the carrier policy was NOT widened as a `pg_policies` catalog fact), +**29** integration (**157** — §27's *carrier flow* end to end, the eighth of the eleven), +**42** e2e (**229**); page count 363 → **368**. **Five injected defects each failed loudly**, one of them instructively: widening M-72's actor gate did not break the action test (§13's own list is an independent second barrier) — what failed were the two assertions whose job is to compare the layers. **Two real defects the lanes caught pre-ship**: a refusal pre-check that refused every legitimate "delivered" because §20's delivery timestamp is a fact about the REQUEST, and a cross-file flake where random tracking numbers collided with M-75's `ilike 'PL%11'` injection control. The **driver-token threat model** is documented in full, including what is NOT mitigated |
| M-77 | B | [M-77-shipment-documents.md](M-77-shipment-documents.md) | **§16 documents + the POD workflow, and the restored VISIBILITY MATRIX.** Migration **0024**: a SECOND private bucket (`shipment-docs`) rather than a namespace inside `carrier-docs` — argued, because 0004 authorizes BY CARRIER through a folder prefix and a shipment document has up to four legitimate readers whose access depends on `doc_type`/`status`/`visibility`, columns a storage policy would have to join; reusing it would mean loosening the highest-PII bucket in the product or leaving objects where the policies cannot express who may read them. **The matrix is DATA, twice**: `DOCUMENT_AUDIENCES` (a full `Record`, so a twelfth document type is a compile error until its audience is stated) and `shipment_document_audiences` (22 rows RLS joins), pinned cell-for-cell by an integration test — drift between them is the one bug neither the unit lane (no database) nor the RLS lane (no TypeScript) could see. **No document type is public** — three ways: no seeded row, a CHECK forbidding one, and `documentTypesForAudience("public") === []`. **`visibility` NARROWS and never widens**: `trg_shipment_documents_visibility` raises PL422 on any value the matrix withholds, so §4's *"never show carrier rate confirmations"* is a write failure rather than a code review. **§12's `broker` band is live** — the value M-70 added for exactly this, with the "when authorized" being `shipments.broker_partner_id` through the already-`active`-filtered `my_broker_partner_ids()`; M-81 still owns the SURFACE, and the dispatcher page says so. **§20's POD precondition, completed** — 0019 shipped `shipment_transition_facts()` with `approved_pod_document_id` as a literal null and the replacement SQL in the comment above it, addressed to M-77 by name; 0024 uses that expression **verbatim** (not "improved" with `and status = 'approved'`, because 0024's CHECK makes them the same condition), and `transitions.ts`/`apply-transition.ts` are not edited at all — which was 0019's promise. **The headline regression-to-green**: upload → still refused · approve → **succeeds** · un-approve → refused again, because `review_shipment_document()` clears `approved_at` on any non-approval, so the fact tracks the CURRENT decision. Uploads are magic-byte sniffed (`sniffMime`, S-03), size-capped, randomized under a CHECK-enforced shipment prefix, and gated by a **per-ROLE doc-type allow-list** in three independent places (separate Zod schemas so a carrier's "invoice" is never even parsed as valid · `canUpload()` · `service_role`-only EXECUTE) — driver gets `bol`/`pod`, carrier those plus the accessorial evidence only they produce, staff all eleven, shipper and broker **none**. Every upload and every approval is a `shipment_events` row in the SAME transaction (upload `staff_only`, approval at the widest licensed band, rejection staff-only). **§15 document-access history** reuses M-61/M-69's `document.download` action string, writer and shape, so it is ONE query over `audit_events`; the ≤300s signed URL is never logged, stored, or put in an `href` — it goes straight to `window.open`, and a unit test asserts `signed.signedUrl` appears exactly once in the store. §11's ninth tile stops being `null`: a `security definer` count that returns **a number and nothing else**, scoped by `my_shipper_ids()` internally, because §16 keeps unapproved documents out of customer hands and a plain count would report 0 for a queue of five. +**95** unit (**1061**) incl. a table-driven walk of **44 cells** transcribed from the DIRECTIVE rather than from the code, +**55** RLS (**502** — eight documents in eight matrix states, so shipper "3 of 8" and broker "2 of 8" are band decisions), +**37** integration (**194** — §27's *document upload* and *POD upload*, the eleventh and ninth of the eleven), +**11** e2e (**240**). **Seven injected defects each failed loudly**, including both directions of matrix drift. Page count unchanged at **368** |

| M-78 | B | [M-78-eta-exceptions.md](M-78-eta-exceptions.md) | **§10's ETA architecture and §21's exceptions — the two tables M-71 deferred.** Migration **0025**: `shipment_eta_history` (append-only, staff-only, written in the SAME transaction as the column and the `eta_update` event, so an ETA that moved without its previous value preserved is not a reachable state) and `shipment_exceptions` (§21's 13 types, its 10 fields, plus `source_event_id`/`resolution_event_id` — argued, not assumed). **§21's one non-negotiable is a DATABASE property, not a projection string**: a row policy cannot restrict a column and staff share `authenticated` with customers, so the base table carries the STAFF policy alone and the customer path is `my_shipment_exceptions()`, whose `returns table (…)` is a seven-column allow-list with no `internal_description` and no `resolution` in it — the RLS suite reads that OUT list out of `pg_proc`, so an `alter function` that widened it fails even though every row assertion would still pass. The audience is resolved from the caller's own memberships INSIDE the function, never from an argument. **The M-75/M-76 backfill, honoured**: `backfill_shipment_exceptions()` migrates every `m75_event_only` / `m76_carrier_report` / `m76_driver_report` event into a row field for field, **deletes nothing** (0019's append-only trigger enforces that independently), is idempotent on a unique `source_event_id`, and SKIPS an event whose type is not a §21 value rather than filing a fiction as `other`. Three assertions that were TRUE are now FALSE, each inverted in place rather than deleted. **Which ETA sources are real, stated plainly**: `manual` and `dispatcher_adjusted` (a human typed it — §30's "ETA provided by dispatcher"); **`calculated` is now REAL** — the SERVER computes it from `distance_miles` at 50 mph planning speed plus 49 CFR §395.3 rest and 2 h dock dwell each end, DISCARDING whatever the operator typed, refusing outright with §26's `eta_calculation_failure` when there is no mileage, capping its own confidence at `medium` because `high` belongs to an observed ETA, and rendering under a SEVENTH honest label ("Estimated from distance and standard transit times") because putting it under the dispatcher label would be a lie in the other direction; **`provider` stays UNREACHABLE** — no adapter exists, M-80 owns them, and `DISPATCHER_ETA_SOURCES ∪ UNREACHABLE_ETA_SOURCES = ETA_SOURCES` is asserted as a partition so a future module must move it in the same commit that makes it true. §10's "notify the customer" calls M-60's SHIPPED `notifyCustomer` with `email` omitted: the in-portal row is real today, the localized email is M-79's and is not faked; pickup ETAs and replays notify nobody. D-6 EXTENDED rather than duplicated — a fourth group (`resolution.*` ×8) and three delay phrases in the same object, same token prefix, same resolver, so `/track` picked them up with no code change; `PHRASE_GROUPS` is data now. The §21 banner M-73 passed as `exceptions: []` is wired on `/track` (fails SOFT, argued) and the shipper detail page; the dispatcher gets the register with open/triage/resolve, and a carrier's or driver's report opens a real row at fixed `medium` severity with NOTHING published to the customer. +**87** unit (**1148**), +**50** RLS (**552**), +**28** integration (**222** — §27's eleventh named test, *exception lifecycle*), +**13** e2e (**253**). **Eight injected defects each failed loudly**, including a widened OUT list, a fallback distance, `provider` on the form, and a backfill that deleted its sources. Page count unchanged at **368** |

| M-79 | B | [M-79-shipment-notifications.md](M-79-shipment-notifications.md) | **Closes Phase B — §17's notifications, and §25's restored *background processing architecture*.** Migration **0026**, additive (0001–0004 frozen; 0017–0025 untouched; 0005 gains three defaulted `user_preferences` columns and loses nothing): 3 enums, 5 tables, 4 `service_role`-only `security definer` functions. The architecture is the point — **an inline send has nowhere to retry from**, so §17's *retry handling*, *idempotency keys*, *log notification attempts* and *record provider response* become a `shipment_notification_queue` (unique `idempotency_key`; a payload-safety CHECK refusing `signed_url`/`access_code`/`internal_message`/`gross_shipper_amount`/`carrier_pay` at the WRITER), an append-only `shipment_notification_attempts` ledger, a harvest watermark, and `/api/cron/notifications` guarded exactly like `/api/cron/daily` (bearer `CRON_SECRET`, **constant-time**, 503 unset / 401 wrong) on a `*/5` schedule. **No second event source**: the queue is filled by HARVESTING `shipment_events` — M-72's transitions, M-78's ETA changes and exceptions, M-77's POD approval, M-31/0021's shipper invoices — against a rules TABLE, so a notification cannot be missed because a call site was forgotten; M-75's *resend* is the one deliberate human enqueue and goes through the same preference gate. The event→notification mapping exists in SQL **and** TypeScript and the integration lane pins them cell-for-cell in both directions (M-77's anti-drift technique). §17's 11 templates ride M-60's `CustomerEmail` layout ×5 locales (en/es/fr authored, ru/ht mirrored + flagged), each carrying the tracking link with **only the first factor** (M-73's threat model: an email outlives a location bar) and §30's *milestone tracking* label — never "live". D-6's phrase library is RESOLVED for email rather than re-authored: a raw `phrase:delay.traffic` in a customer's inbox was a real defect, fixed here with 6 tests, 4 of which fail on reversion. Preferences are two-level (account boolean + address suppression) and checked at enqueue **and** again at send, because the later check is the authoritative one; a suppression settles TERMINAL-SUCCESS, never as a failure. Tokenized `/notifications/unsubscribe` — no session, `noindex`, GET never mutates. Degrades honestly: no Resend key → `skipped`, never `sent`. 90 unit · 41 integration · 36 RLS · 11 e2e |

| M-80 | C | [M-80-map-providers.md](M-80-map-providers.md) | **Opens Phase C.** §9's map and provider-adapter architecture — and the sentence this module is judged by: **NO PROVIDER IS CONNECTED.** PickLoads holds no telematics contract, no API credentials and no ELD consent; nothing opens a socket to Motive, Samsara, Geotab or Verizon Connect; `tracking_provider_connections` is empty in every environment; and every customer surface therefore renders §30's *"Milestone tracking"*, which is the honest state rather than a placeholder. What ships is the INTERFACE §9 asks for, with a **typed refusal** that distinguishes `not_configured` (no env vars) from `not_implemented` (env vars present, no transport) — closing the trap where an operator sets `MOTIVE_API_KEY` and believes tracking started. Five adapters, one file each, because the differences are the argument: **Geotab reports km/h** (a generic adapter puts a truck at 105 mph on a 105 km/h motorway), **Verizon Connect's `updateUtc` carries no zone** (`Date.parse` reads it server-local and the truck lands four hours out), **Samsara's only city is `"Richmond, VA"`** (stored whole it renders as `"Richmond, VA, VA"`). `normalize()` is IMPLEMENTED FOR REAL per vendor — the piece that makes "adding a provider needs no rewrite" checkable — and the registry is a full `Record<TrackingProvider, …>`, so a sixth enum value is a compile error until its file exists. Migration **0027**, additive, nothing shipped altered: `shipment_locations` (M-70's row type in full, incl. §9's **vehicle speed** and **raw provider metadata**) and `tracking_provider_connections` (§9 Mode B's five fields incl. the **`tracking_url`** plan §4 found missing). **Why a second location table:** §9 requires configurable retention and 0019 made the event ledger append-only for every role INCLUDING the owner, so a retention window over it is a sentence in a document — 0027 therefore REFUSES coordinates on `shipment_events` (PL422, breaking nothing: no path in `src/` ever wrote one) and MIRRORS any event carrying a city into the purgeable series, so §9 Mode A produces real history with no call-site change anywhere. **The restored RETENTION EXECUTOR**: `purge_expired_shipment_locations()` on the `location_retention_days` switchboard key (seeded 90), two predicates so SHORTENING the window bites immediately while lengthening resurrects nothing, bounded with `for update skip locked`, **fails safe to 90 and never to "keep forever"**, wired as task 3 of the existing `CRON_SECRET`-guarded `/api/cron/daily` rather than a second cron entry to forget — and **proved by deletion** in both the RLS and integration lanes (expired row gone, fresh row kept). §9's **four levels end to end**: the read side extended to the series with the public audience capped at city/state EVEN AT `exact` (§9 verbatim), `hidden` and `milestone_only` rendered identically to "no readings yet" so the setting is not a signal, and **speed as a SEPARATE permission** — `exact` AND driver consent, asserted in both directions; the write side is **directional** (narrowing = dispatcher, widening = ADMIN, ranked 0–3) because §15 puts visibility control on the admin list while a literal reading would make turning a map OFF the slow action — enforced in the app for the MESSAGE and in 0027 as the AUTHORITY (PL403), with the rank map read out of the SQL by a unit test. **The coordinate-disclosure decision M-70 deferred**: customer event DTOs still carry NO lat/long — positions live in the purgeable series because the ledger cannot be purged, because band (content) and level (precision) are orthogonal controls, and because a tracking number plus a ZIP is not an account; `raw_metadata` goes further and is serialized by NO audience, staff included. §15's credential line drawn precisely: an API key belongs in an env var (a CHECK refuses eight credential shapes in the URL), a Mode B share link IS the resource §9 says to store — https-only, staff-only at every layer, one active per shipment, revocation one-way. **The map**: inline SVG, **zero network requests, so the CSP is unchanged** — updated for exactly what was needed, which was nothing; no third party learns which trucks are watched; labelled a *route diagram* because it draws no roads; **lazy-loaded** through `next/dynamic(ssr:false)` with the e2e lane reading `.next/static/chunks` to prove it is in no route or shared chunk and is never fetched; **§23's accessible alternative is a VISIBLE ordered list** with `<time datetime>` present whether the map mounts or not, whose summary IS the map's `<desc>`; reduced motion is the DEFAULT (the one animation is inside `prefers-reduced-motion: no-preference`). **`eta_source='provider'` stays UNREACHABLE** and M-78's partition assertion stays truthful — `fetchEtaInputs` returns inputs, never an ETA claim, and no M-80 module contains the string `eta_source`. §26's `location_provider_failure` emitted on six paths, never carrying the URL. +**161** unit (**1399**), +**83** RLS (**671**), +**32** integration (**295**), +**6** e2e (**270**); 373 pages unchanged. **Eight injected defects each failed loudly** (public cap removed · an adapter returning data · retention defaulting to forever · the admin widening check deleted · the purger deleting nothing · a static map import · a widened OUT list · an anon policy). One incidental fix named rather than smuggled: the sitewide `.langsel` met WCAG 2.5.8 only under `pointer:coarse`, found by M-80's own 320px scan |
| M-81 | C | [M-81-broker-partner-access.md](M-81-broker-partner-access.md) | **§12's broker-partner access, and the plan's §4 RESTORED requirement — the allow/deny permission lists.** Migrations **0028** (one statement: the `broker` value on `user_role`, in its own file because PostgreSQL refuses to *use* an enum value added in the same transaction, so a runner that wraps a migration would fail at 0029's first mention of it) and **0029**. **The enum value grants NOTHING**: no policy anywhere reads `profiles.role = 'broker'` — asserted as a catalog fact — and the fixtures prove both halves, with brokers A/B/C reading their linked shipments on the enum's DEFAULT role while broker F carries `'broker'` and reads nothing. What it buys is what M-71 had no surface for: an invitation that assigns a role (M-58's idiom), and a `portalHomeFor()` branch without which an invited partner enters a redirect loop between `requireCarrier` and its own home. **§12's allow/deny lists as DATA** (M-77's precedent, restored requirement): `BROKER_FIELD_POLICY` is a full `Record` over all **53** `ShipmentRow` columns — **42 allowed, 11 denied** — so a column added by any later module is a COMPILE ERROR until somebody states whether a partner may see it; every deny cell carries the §12 clause it serves, and the three denials §12 does *not* name (`counterparty_identity`, `internal_operations`, `access_credential`) are their own reasons rather than filed under a clause they do not belong to. `carrier_pay` is denied under **PickLoads commission** — M-70's rule restated, *"not even `carrier_pay`"*, because price + pay computes it; `carrier_id` reaches the DTO as the **boolean** `carrier_assigned`, answering §1's "assigned carrier status" without opening §12's carrier packet. `BROKER_DENIED_SOURCES` names ten off-`shipments` sources, each pinned to the MIGRATION that refuses it, because a TypeScript allow-list controls what a serializer emits and nothing about what a hand-written query can fetch. **§12's two grant shapes are two tables** — `broker_shipment_grants` (one live row per (shipment, partner), revocation as a COLUMN so §15 can answer "who could see this last March?") and `broker_account_agreements` (`shipper_id` **NOT NULL**, so §19's forbidden wildcard is unrepresentable rather than merely unused) — OR'd with M-71's floor inside ONE function, `broker_can_read_shipment()`, so the database holds exactly one definition of the question. **M-71's policies were EXTENDED, never rewritten**, per 0018's own instruction (*"it cannot widen it without a new policy that says so"*): four new SELECT-only policies sit beside the shipped four, and because `my_broker_partner_ids()` is REPLACED to require `active` **AND** `verification_status = 'verified'` (§12's *"verified"*, now a database fact inherited by every policy in the chain in one write), **M-81 net-TIGHTENS the floor it extends**. §3's *"no public self-registration"* is a property of the SCHEMAS: no broker schema has a `role`, `verification_status` or `active` field, the accept schema has no organization id either, and `role: "broker"` exists as a literal in **exactly one place** — counted by test and located inside the public accept action. The invite is M-58's idiom (SHA-256 hash only, single-use, 7-day expiry) plus §12's two additions: it names the ORGANIZATION, and it is revocable, with a CHECK refusing a row that is both accepted and cancelled; `token_hash` is granted at COLUMN level so naming it is a permission error for **every** session, staff included. **The partner portal is one nav entry** — §12 grants a VIEW and nothing else, so there is no documents page, no billing page (§12 forbids it) and no company page — and the detail surface contains **not one `<form>`**, because §19 gives a broker SELECT and 0018/0029 grant no customer write policy for a control to call. Three honest empty states, not one: no membership · invited but UNVERIFIED · verified with nothing shared yet. **M-77's caveat REMOVED, not softened** — the dispatcher document note that said *"the permission exists, the surface does not"* is replaced by the live behaviour, and `getBrokerDocumentUrlAction` (which M-77 shipped early for exactly this) is called rather than re-implemented. Plan §9.3's vetting checklist lands as five columns, with "under 12 months old" surfaced as a FACT beside the authority date and **never as a verdict** (§30). +**63** unit (**1462**), +**71** RLS (**742**), +**34** integration (**329**), +**13** e2e (**283**); page count 373 → **388**. **Three injected defects each failed a DIFFERENT lane** (a widened matrix cell → unit; the verification clause deleted → RLS; grant revocation ignored → RLS *and* integration) — and the third is why the integration lane carries a policy-level probe with the application id filter removed: on the first attempt the injection passed, because the app filter masked the policy. Four residual risks named, including the one place `shipper_id` is read on a broker path and why the value never leaves the function |
| M-82 | C | [M-82-responsive-a11y-qa.md](M-82-responsive-a11y-qa.md) | **§22 + §23 QA and remediation for every tracking surface — no route, no table, no migration, no env var, no feature.** The plan's §4 records the seven-breakpoint suite as an *unstated downgrade* from §22's twelve; closing it mattered less for the five extra pixel counts than for what auditing at all twelve **in a browser** exposed. **The root cause of eleven of the twelve defects below is one methodological gap**: M-73→M-81 each scanned with axe-core in **jsdom**, which applies no stylesheet — nothing has a width, nothing scrolls, nothing has a computed colour — so for a brief that is entirely *overflow, clipping, touch-target geometry, date-input intrinsic width, oversized maps* none of it was observable, and six consecutive modules shipped an honest note saying layout was unproven. M-82's move is a **browser harness**: the a11y suites already render every surface in every state from the real components, so they now also write that DOM to disk (`tests/harness/emit.ts`), and Playwright loads each file into Chromium behind the stylesheets the running server **actually links** (verified, not assumed) at all twelve widths. Same markup, same CSS, real layout engine — and it claims nothing more: no server data, no hydration, no client navigation, with `responsive.spec.ts` still **asserting** the session gate that makes the split necessary. **29 surface-states** — `/track` lookup+result (5 states), shipper tiles/list/detail (7), dispatcher board/search/column/**create**/detail (7), carrier list/detail (3), driver granted+expired (2), broker list/detail (2), map mounted+text-only (2) — each with §22's **eleven prohibitions as eleven assertions** at every width, plus §22's **mobile priority order** (`data-prio` markers, asserted at all twelve) and §23's thirteen requirements. Sampling is **stated**: layout probes run at all twelve widths and are not sampled; axe runs at 320/768/1440 because those are the three *arrangements* the stylesheets produce, and the twelve-width probe is what proves no fourth exists. **Twelve defects found and fixed.** **D-2**: M-59's card transform *never actually shrank the table* — `tr` became a block and `td` a flex row but `table`/`tbody` stayed table boxes, so table min-content sizing survived and the documents table sat at 371px and the dispatcher detail at 395px on a 320px screen (**36–75px of page overflow**), the exact failure `.ptable--cards` exists to prevent. **D-8**: `.sr-only` is `position:absolute` and `.ptable-wrap` was *not positioned*, so hidden column labels resolved against the **initial** containing block, landed at x=346 and pushed the document **27px** wide — a real scrollbar produced entirely by text nobody can see (`overflow` clips in-flow content; it does not clip an abspos descendant whose containing block is outside the scroller). **D-1**: `.field` controls render at 15.04px and iOS Safari zooms — and scrolls sideways — on any focused field under 16px, which is the mechanism behind §22's named *"no iOS date-input overflow"*; M-76 set 16px on the driver page for exactly this reason and the shared vocabulary never did. **D-6** (WCAG 2.1.1, axe *serious*): the provider-adapter table, the exception table and the whole board strip when empty or failed scroll horizontally with **no focusable content**, so a keyboard user cannot reach them at all — invisible in jsdom because nothing there ever scrolls; fixed with a `ScrollRegion` component carrying M-59's `.flow` pattern at all 12 tracking scroll containers. **D-9**: the driver page mounted its `role="alert"`/`role="status"` **at the same moment their text arrived** — a live region must be in the tree *before* its contents change — on the one surface whose entire purpose is a confirmation to someone who cannot look at the screen; M-73's public forms already had the correct shape, so this was the outlier. **D-10** (§22 *no hidden actions*): `Start Carrier Setup` → `/#quote` sits in `.nav-cta`, hidden ≤960px, with **no drawer or footer equivalent by destination** — unreachable on every phone-width page; fixed in the nav rather than by widening the test's exemption, which is the point of measuring by destination. **D-12**: the **map sat above support and documents** on both `/track` and the shipper detail — §22's *last* priority above its *fifth* and *sixth*, two thumb-scrolls of route diagram between "where is my freight" and "how do I ask a human". Plus **D-3** (`input[type=file]` at its intrinsic 356px overflowing by 36px; a 22px-tall select), **D-4** (unbreakable operator free text, 15px), **D-5** (`.psh-pager` links at 46×19 — under 2.5.8 at *every* width, not just `pointer:coarse`, the same class of miss M-80 caught on `.langsel`), **D-7** (board columns `h3` under `h1` while the expanded column view already used `h2`; axe's `heading-order` is best-practice, not A/AA, which is why it survived), **D-11** (`.field input{width:100%}` drew checkboxes as **242×13 bars** — M-74 wrote the fix but scoped it to `.psh-filters .psh-toggle`). **Where the audit found nothing, it says so**: the driver page's layout (designed at 320px, passed unchanged), M-80's map and its text alternative, **zero** hover-only reveals across every stylesheet, zero running animations under `prefers-reduced-motion`, zero `color-contrast` violations (the first *measured* claim on the portal tracking surfaces — jsdom cannot compute a colour), still zero modals app-wide, and `.ptable--cards` + `data-th` on **every** customer table, now a standing assertion. **Eight limits stated plainly**, incl. Chromium-only (the iOS fix follows Safari's documented rule, not an observation of it), axe sampled by arrangement, non-tracking staff tables left with the same D-6 defect and named as the next sweep. +**6** unit (**1468**), +**77** e2e (**360**: 70 in the new suite, 7 in `axe.spec.ts` for `/track` ×5 locales, the live error state and the expired-token refusal ×5); RLS **742** and integration **329** unchanged — nothing schema-side was touched; **388** pages. `playwright.config.ts` gains a `globalSetup` that regenerates the fixtures with **no skip switch**, because a gate that only passes when two commands are run in the right order will eventually be run in the wrong one |

Phases: 0 = foundations · 1 = public site · 2 = onboarding/CRM · 3 =
loads/billing/content · H = hardening · U = upgrade directive (M-50a audit) ·
A = integrity prerequisite (FINAL-IMPLEMENTATION-PLAN phase A) · B = tracking
core (FINAL-IMPLEMENTATION-PLAN phase B) · C = tracking completion
(FINAL-IMPLEMENTATION-PLAN phase C).
