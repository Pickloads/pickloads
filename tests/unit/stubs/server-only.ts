/**
 * Test stub for the `server-only` package (which throws when imported outside
 * a React Server Components context). Aliased in vitest.config.ts so server
 * modules (crypto, rate-limit, turnstile, guards) are unit-testable as-is.
 */
export {};
